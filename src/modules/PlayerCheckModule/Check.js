module.exports = class {
    constructor(client, options = {}) {
        this.client = client
        this.name = options.name;
        this.id = options.id;
    }

    async run(playerData) {

    }
}