const Check = require("../Check");

module.exports = class extends Check {

    constructor(client) {
        super(client, {
            name: "Bedwars Quad 1"
        });

    }

    async run(playerData) {

    }
}